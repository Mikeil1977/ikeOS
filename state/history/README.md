# MikeOS History

Store dated snapshots of distilled cockpit state here when `state/today.md` would otherwise become noisy.

File name format:

`YYYY-MM-DD.md`

History files should contain daily cards and source freshness only. Do not store raw evidence.
